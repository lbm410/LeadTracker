# Frontend environment (used only when running Vite outside Docker).
# Inside docker-compose these are injected automatically.

# Base URL of the API as reachable from your browser.
VITE_API_URL=http://localhost:8000

# Must match API_KEY in the backend .env
VITE_API_KEY=dev-local-key-change-me
